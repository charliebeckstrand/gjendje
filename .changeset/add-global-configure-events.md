---
'gjendje': minor
---

Add five new global configure events: `onChange`, `onReset`, `onIntercept`, `onValidationFail`, and `onExpire`. Split the configure docs into separate Options and Events tables.
