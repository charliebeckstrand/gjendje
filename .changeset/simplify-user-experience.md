---
'gjendje': minor
---

Add scope shortcuts (`local`, `session`, `url`, `bucket`), three-argument `state()` overload, and `memory` scope alias to simplify the API surface and reduce the learning curve.
