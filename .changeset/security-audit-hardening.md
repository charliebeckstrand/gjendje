---
'gjendje': patch
---

Security audit hardening: fix URL adapter double-encoding, prevent migration infinite loops from corrupted version numbers, harden BroadcastChannel message validation, use Object.hasOwn in pickKeys, reject non-safe-integer version envelopes.
